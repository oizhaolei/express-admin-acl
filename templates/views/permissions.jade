extends ../layouts/default

block content
  script#jplist-template(type="text/x-handlebars-template")
    thead
      tr
        td Permission
        td Action
    {{#each this}}
    tr
      td
        p(class="title") {{permission}}
      td
        a(href="/permissions/{{_id}}") Detail
    {{/each}}

  // Wrapper
  table#demo(class="table table-striped")
  ul#pagination(class="pagination-sm")

  //- scripts
  script(src='/js/views/permissions.js')
